// Generated by CoffeeScript 1.8.0

/* (C) 2014 Narazaka : Licensed under The MIT License - http://narazaka.net/license/MIT?2014 */

(function() {
  var FileSystemObject, Promise, path,
    __slice = [].slice;

  path = this.path;

  Promise = this.Promise;

  if (typeof require !== "undefined" && require !== null) {
    if (path == null) {
      path = require('path');
    }
    if (Promise == null) {
      Promise = require('bluebird');
    }
  }

  FileSystemObject = (function() {
    var current_new_path_methods, fd_methods, method, single_path_methods, single_path_methods_nosync, _i, _j, _k, _l, _len, _len1, _len2, _len3;

    function FileSystemObject(fs, newPath) {
      this.fs = fs;
      this.path = path.resolve(newPath);
    }

    single_path_methods = ['truncate', 'chown', 'lchown', 'chmod', 'lchmod', 'stat', 'lstat', 'readlink', 'realpath', 'unlink', 'rmdir', 'mkdir', 'readdir', 'utimes', 'readFile', 'writeFile', 'appendFile', 'exists'];

    single_path_methods_nosync = ['createReadStream', 'createWriteStream'];

    current_new_path_methods = ['rename', 'link', 'symlink'];

    fd_methods = ['ftruncate', 'fchown', 'fchmod', 'fstat', 'write', 'read'];

    for (_i = 0, _len = single_path_methods.length; _i < _len; _i++) {
      method = single_path_methods[_i];
      FileSystemObject.prototype[method] = (function(method) {
        return function() {
          var args, _ref;
          args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
          if (args[args.length - 1] instanceof Function) {
            return (_ref = this.fs)[method].apply(_ref, [this.path].concat(__slice.call(args)));
          } else {
            return new Promise((function(_this) {
              return function(resolve, reject) {
                var _ref1;
                return (_ref1 = _this.fs)[method].apply(_ref1, [_this.path].concat(__slice.call(args), [function() {
                  var args, err;
                  err = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
                  if (err != null) {
                    return reject(err);
                  } else {
                    return resolve(args);
                  }
                }]));
              };
            })(this));
          }
        };
      })(method);
      FileSystemObject.prototype[method + 'Sync'] = (function(methodSync) {
        return function() {
          var args, _ref;
          args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
          return (_ref = this.fs)[methodSync].apply(_ref, [this.path].concat(__slice.call(args)));
        };
      })(method + 'Sync');
    }

    for (_j = 0, _len1 = single_path_methods_nosync.length; _j < _len1; _j++) {
      method = single_path_methods_nosync[_j];
      FileSystemObject.prototype[method] = (function(method) {
        return function() {
          var args, _ref;
          args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
          if (args[args.length - 1] instanceof Function) {
            return (_ref = this.fs)[method].apply(_ref, [this.path].concat(__slice.call(args)));
          } else {
            return new Promise((function(_this) {
              return function(resolve, reject) {
                var _ref1;
                return (_ref1 = _this.fs)[method].apply(_ref1, [_this.path].concat(__slice.call(args), [function() {
                  var args, err;
                  err = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
                  if (err != null) {
                    return reject(err);
                  } else {
                    return resolve(args);
                  }
                }]));
              };
            })(this));
          }
        };
      })(method);
    }

    for (_k = 0, _len2 = current_new_path_methods.length; _k < _len2; _k++) {
      method = current_new_path_methods[_k];
      FileSystemObject.prototype[method] = (function(method) {
        return function() {
          var args, current_path, newPath, new_path, _ref;
          newPath = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
          current_path = this.path;
          new_path = path.resolve(path.dirname(this.path), newPath);
          if (args[args.length - 1] instanceof Function) {
            return (_ref = this.fs)[method].apply(_ref, [current_path, new_path].concat(__slice.call(args)));
          } else {
            return new Promise((function(_this) {
              return function(resolve, reject) {
                var _ref1;
                return (_ref1 = _this.fs)[method].apply(_ref1, [current_path, new_path].concat(__slice.call(args), [function() {
                  var args, err;
                  err = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
                  if (err != null) {
                    return reject(err);
                  } else {
                    return resolve(args);
                  }
                }]));
              };
            })(this));
          }
        };
      })(method);
      FileSystemObject.prototype[method + 'Sync'] = (function(methodSync) {
        return function() {
          var args, current_path, newPath, new_path, _ref;
          newPath = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
          current_path = this.path;
          new_path = path.resolve(path.dirname(this.path), newPath);
          return (_ref = this.fs)[methodSync].apply(_ref, [current_path, new_path].concat(__slice.call(args)));
        };
      })(method + 'Sync');
    }

    for (_l = 0, _len3 = fd_methods.length; _l < _len3; _l++) {
      method = fd_methods[_l];
      FileSystemObject.prototype[method] = (function(method) {
        return function() {
          var args, _ref;
          args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
          if (args[args.length - 1] instanceof Function) {
            return (_ref = this.fs)[method].apply(_ref, [this.fd].concat(__slice.call(args)));
          } else {
            return new Promise((function(_this) {
              return function(resolve, reject) {
                var _ref1;
                return (_ref1 = _this.fs)[method].apply(_ref1, [_this.fd].concat(__slice.call(args), [function() {
                  var args, err;
                  err = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
                  if (err != null) {
                    return reject(err);
                  } else {
                    return resolve(args);
                  }
                }]));
              };
            })(this));
          }
        };
      })(method);
      FileSystemObject.prototype[method + 'Sync'] = (function(methodSync) {
        return function() {
          var args, _ref;
          args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
          return (_ref = this.fs)[methodSync].apply(_ref, [this.fd].concat(__slice.call(args)));
        };
      })(method + 'Sync');
    }

    FileSystemObject.prototype.open = function() {
      var args, _ref;
      args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
      if (args[args.length - 1] instanceof Function) {
        return (_ref = this.fs).open.apply(_ref, [this.path].concat(__slice.call(args.slice(0, -1)), [(function(_this) {
          return function(err, fd) {
            _this.fd = fd;
            return args[args.length - 1](err, fd);
          };
        })(this)]));
      } else {
        return new Promise((function(_this) {
          return function(resolve, reject) {
            var _ref1;
            return (_ref1 = _this.fs).open.apply(_ref1, [_this.path].concat(__slice.call(args), [function(err, fd) {
              _this.fd = fd;
              if (err != null) {
                return reject(err);
              } else {
                return resolve([fd]);
              }
            }]));
          };
        })(this));
      }
    };

    FileSystemObject.prototype.openSync = function() {
      var args, _ref;
      args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
      return this.fd = (_ref = this.fs).openSync.apply(_ref, [this.path].concat(__slice.call(args)));
    };

    FileSystemObject.prototype.close = function(callback) {
      if (callback != null) {
        return this.fs.close(this.fd, (function(_this) {
          return function(err) {
            if (err == null) {
              delete _this.fd;
            }
            return callback(err);
          };
        })(this));
      } else {
        return new Promise((function(_this) {
          return function(resolve, reject) {
            return _this.fs.close(_this.fd, function(err) {
              if (err != null) {
                return reject(err);
              } else {
                delete _this.fd;
                return resolve([]);
              }
            });
          };
        })(this));
      }
    };

    FileSystemObject.prototype.closeSync = function() {
      var ret;
      ret = this.fs.closeSync(this.fd);
      delete this.fd;
      return ret;
    };

    FileSystemObject.prototype.mkdirp = function() {
      var args, mkdir;
      args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
      if (args[args.length - 1] instanceof Function) {
        mkdir = (function(_this) {
          return function(dir, callback) {
            return fs.stat(dir, function(err) {
              if (err != null) {
                return mkdir(path.dirname(dir), function(err) {
                  var _ref;
                  if (err != null) {
                    return callback(err);
                  } else {
                    return (_ref = _this.fs).mkdir.apply(_ref, [dir].concat(__slice.call(args.slice(0, -1)), [callback]));
                  }
                });
              } else {
                return callback();
              }
            });
          };
        })(this);
        return mkdir(this.path, args[args.length - 1]);
      } else {
        mkdir = (function(_this) {
          return function(dir) {
            return new Promise(function(resolve, reject) {
              return fs.stat(dir, function(err) {
                if (err != null) {
                  return mkdir(path.dirname(dir)).then(function() {
                    var _ref;
                    return (_ref = _this.fs).mkdir.apply(_ref, [dir].concat(__slice.call(args), [function(err) {
                      if (err != null) {
                        return reject(err);
                      } else {
                        return resolve([]);
                      }
                    }]));
                  });
                } else {
                  return resolve([]);
                }
              });
            });
          };
        })(this);
        return mkdir(this.path);
      }
    };

    FileSystemObject.prototype.mkdirpSync = function() {
      var args, mkdirSync;
      args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
      mkdirSync = (function(_this) {
        return function(dir) {
          var _ref;
          try {
            return fs.statSync(dir);
          } catch (_error) {
            mkdirSync(path.dirname(dir));
            return (_ref = _this.fs).mkdirSync.apply(_ref, [dir].concat(__slice.call(args)));
          }
        };
      })(this);
      return mkdirSync(this.path);
    };

    FileSystemObject.prototype.mkpath = FileSystemObject.prototype.mkdirp;

    FileSystemObject.prototype.mkpathSync = FileSystemObject.prototype.mkdirpSync;

    FileSystemObject.prototype.readdirAll = function(callback) {
      var basedir, readdir;
      if (callback != null) {
        readdir = (function(_this) {
          return function(dir, basedir, callback) {
            return _this.fs.readdir(dir, function(err, entries) {
              var done, entry, entry_paths, push_entry_paths, _len4, _m, _results;
              if (err != null) {
                return callback(err);
              }
              entry_paths = [];
              done = 0;
              push_entry_paths = function(a_entry_paths) {
                entry_paths = entry_paths.concat(a_entry_paths);
                done++;
                if (done === entries.length) {
                  return callback(void 0, entry_paths);
                }
              };
              _results = [];
              for (_m = 0, _len4 = entries.length; _m < _len4; _m++) {
                entry = entries[_m];
                _results.push((function(entry_path) {
                  return _this.fs.stat(entry_path, function(err, stats) {
                    if (err != null) {
                      return callback(err);
                    }
                    if (stats.isDirectory()) {
                      return readdir(entry_path, basedir, function(err, entry_paths) {
                        if (err != null) {
                          return callback(err);
                        }
                        return push_entry_paths([entry_path.replace(basedir, '')].concat(__slice.call(entry_paths)));
                      });
                    } else {
                      return push_entry_paths([entry_path.replace(basedir, '')]);
                    }
                  });
                })(path.join(dir, entry)));
              }
              return _results;
            });
          };
        })(this);
        basedir = RegExp('^' + this.path.replace(/(\W)/g, '\\$1') + '[\\\\/]?', 'i');
        return readdir(this.path, basedir, callback);
      } else {
        readdir = (function(_this) {
          return function(dir, basedir) {
            return new Promise(function(resolve, reject) {
              return _this.fs.readdir(dir, function(err, entries) {
                var entry, promises, _len4, _m;
                if (err != null) {
                  return reject(err);
                }
                promises = [];
                for (_m = 0, _len4 = entries.length; _m < _len4; _m++) {
                  entry = entries[_m];
                  promises.push((function(entry_path) {
                    return new Promise(function(resolve, reject) {
                      return _this.fs.stat(entry_path, function(err, stats) {
                        if (err != null) {
                          return reject(err);
                        }
                        if (stats.isDirectory()) {
                          return readdir(entry_path, basedir).then(function(_arg) {
                            var entry_paths;
                            entry_paths = _arg[0];
                            return resolve([entry_path.replace(basedir, '')].concat(__slice.call(entry_paths)));
                          });
                        } else {
                          return resolve([entry_path.replace(basedir, '')]);
                        }
                      });
                    });
                  })(path.join(dir, entry)));
                }
                return Promise.all(promises).then(function(entry_paths_list) {
                  var a_entry_paths, entry_paths, _len5, _n;
                  entry_paths = [];
                  for (_n = 0, _len5 = entry_paths_list.length; _n < _len5; _n++) {
                    a_entry_paths = entry_paths_list[_n];
                    entry_paths = entry_paths.concat(a_entry_paths);
                  }
                  return resolve([entry_paths]);
                });
              });
            });
          };
        })(this);
        basedir = RegExp('^' + this.path.replace(/(\W)/g, '\\$1') + '[\\\\/]?', 'i');
        return readdir(this.path, basedir);
      }
    };

    FileSystemObject.prototype.readdirAllSync = function() {
      var basedir, readdir;
      readdir = (function(_this) {
        return function(dir, basedir) {
          var entries, entry, entry_path, entry_paths, stats, _len4, _m;
          entries = _this.fs.readdirSync(dir);
          entry_paths = [];
          for (_m = 0, _len4 = entries.length; _m < _len4; _m++) {
            entry = entries[_m];
            entry_path = path.join(dir, entry);
            stats = _this.fs.statSync(entry_path);
            if (stats.isDirectory()) {
              entry_paths.push(entry_path.replace(basedir, ''));
              entry_paths = entry_paths.concat(readdir(entry_path, basedir));
            } else {
              entry_paths.push(entry_path.replace(basedir, ''));
            }
          }
          return entry_paths;
        };
      })(this);
      basedir = RegExp('^' + this.path.replace(/(\W)/g, '\\$1') + '[\\\\/]?', 'i');
      return readdir(this.path, basedir);
    };

    FileSystemObject.prototype["new"] = function(newPath) {
      var new_path;
      new_path = path.resolve(this.path, newPath);
      return new FileSystemObject(this.fs, new_path);
    };

    FileSystemObject.prototype._new = FileSystemObject.prototype["new"];

    FileSystemObject.prototype.parent = function() {
      var new_path;
      new_path = path.dirname(this.path);
      return new FileSystemObject(this.fs, new_path);
    };

    FileSystemObject.prototype.children = function(callback) {
      if (callback != null) {
        return this.readdir((function(_this) {
          return function(err, entry_paths) {
            var entries, entry_path, _len4, _m;
            if (err != null) {
              return callback(err);
            } else {
              entries = [];
              for (_m = 0, _len4 = entry_paths.length; _m < _len4; _m++) {
                entry_path = entry_paths[_m];
                entries.push(_this["new"](entry_path));
              }
              return callback(void 0, entries);
            }
          };
        })(this));
      } else {
        return this.readdir().then((function(_this) {
          return function(_arg) {
            var entries, entry_path, entry_paths, _len4, _m;
            entry_paths = _arg[0];
            entries = [];
            for (_m = 0, _len4 = entry_paths.length; _m < _len4; _m++) {
              entry_path = entry_paths[_m];
              entries.push(_this["new"](entry_path));
            }
            return entries;
          };
        })(this));
      }
    };

    FileSystemObject.prototype.childrenSync = function() {
      var entries, entry_path, entry_paths, _len4, _m;
      entry_paths = this.readdirSync();
      entries = [];
      for (_m = 0, _len4 = entry_paths.length; _m < _len4; _m++) {
        entry_path = entry_paths[_m];
        entries.push(this["new"](entry_path));
      }
      return entries;
    };

    FileSystemObject.prototype.childrenAll = function(callback) {
      if (callback != null) {
        return this.readdirAll((function(_this) {
          return function(err, entry_paths) {
            var entries, entry_path, _len4, _m;
            if (err != null) {
              return callback(err);
            } else {
              entries = [];
              for (_m = 0, _len4 = entry_paths.length; _m < _len4; _m++) {
                entry_path = entry_paths[_m];
                entries.push(_this["new"](entry_path));
              }
              return callback(void 0, entries);
            }
          };
        })(this));
      } else {
        return this.readdirAll().then((function(_this) {
          return function(_arg) {
            var entries, entry_path, entry_paths, _len4, _m;
            entry_paths = _arg[0];
            entries = [];
            for (_m = 0, _len4 = entry_paths.length; _m < _len4; _m++) {
              entry_path = entry_paths[_m];
              entries.push(_this["new"](entry_path));
            }
            return entries;
          };
        })(this));
      }
    };

    FileSystemObject.prototype.childrenAllSync = function() {
      var entries, entry_path, entry_paths, _len4, _m;
      entry_paths = this.readdirAllSync();
      entries = [];
      for (_m = 0, _len4 = entry_paths.length; _m < _len4; _m++) {
        entry_path = entry_paths[_m];
        entries.push(this["new"](entry_path));
      }
      return entries;
    };

    return FileSystemObject;

  })();

  if ((typeof module !== "undefined" && module !== null ? module.exports : void 0) != null) {
    module.exports = new FileSystemObject(require('fs'), process.cwd());
  } else {
    this.FileSystemObject = FileSystemObject;
  }

}).call(this);
